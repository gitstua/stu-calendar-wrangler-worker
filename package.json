{
  "name": "stucal-92a1",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "generate-key": "/usr/bin/env bash scripts/generate-key.sh"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.4",
    "dotenv": "^16.4.1",
    "vitest": "2.1.8",
    "wrangler": "^3.107.2"
  },
  "dependencies": {
    "luxon": "^3.5.0"
  }
}
